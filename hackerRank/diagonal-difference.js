const diagonalDifference = (arr) => {
  const dim = arr[0].legth;
  let left_diagonal_sum = 0;
  let right_diagonal_sum = 0;
  let sum_total = 0;

  for (let index = 0; (j = dim - 1); j < dim, j > -1, i++, i--) {
    const element = array[index];
    arr[0](i + i);
  }
};
